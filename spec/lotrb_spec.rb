# frozen_string_literal: true

RSpec.describe Lotrb do
  it "has a version number" do
    expect(Lotrb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
